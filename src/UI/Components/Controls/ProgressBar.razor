<div class="progress blazor-ui">
  <div class="progress-bar" style="width: @Value%;">@Percent</div>
</div>

@code {
    private string Percent => $"{Value} %";
    
    [Parameter]
    public int Value { get; set; }

    public void UpdateValue(int value)
    {
        Value = value;
        StateHasChanged();
    }
}